version: "2"
sql:
  - schema: "migrations"
    queries: "queries/iam"
    engine: postgresql
    gen:
      go:
        package: "iamstore"
        out: "../internal/iam/store"
        sql_package: "pgx/v5"
        emit_json_tags: true
        emit_db_tags: true
        emit_pointers_for_null_types: true
        overrides:
          - db_type: "uuid"
            go_type:
              import: "github.com/google/uuid"
              type: "UUID"
          - db_type: "citext"
            go_type: "string"
  - schema: "migrations"
    queries: "queries/platform"
    engine: postgresql
    gen:
      go:
        package: "platformstore"
        out: "../internal/platform/audit/store"
        sql_package: "pgx/v5"
        emit_json_tags: true
        emit_db_tags: true
        emit_pointers_for_null_types: true
        overrides:
          - db_type: "uuid"
            go_type:
              import: "github.com/google/uuid"
              type: "UUID"
          - db_type: "citext"
            go_type: "string"
  - schema: "migrations"
    queries: "queries/events"
    engine: postgresql
    gen:
      go:
        package: "eventsstore"
        out: "../internal/platform/events/store"
        sql_package: "pgx/v5"
        emit_json_tags: true
        emit_db_tags: true
        emit_pointers_for_null_types: true
        overrides:
          - db_type: "uuid"
            go_type:
              import: "github.com/google/uuid"
              type: "UUID"
          - db_type: "citext"
            go_type: "string"
  - schema: "migrations"
    queries: "queries/crypto"
    engine: postgresql
    gen:
      go:
        package: "cryptostore"
        out: "../internal/platform/crypto/store"
        sql_package: "pgx/v5"
        emit_json_tags: true
        emit_db_tags: true
        emit_pointers_for_null_types: true
        overrides:
          - db_type: "uuid"
            go_type:
              import: "github.com/google/uuid"
              type: "UUID"
          - db_type: "citext"
            go_type: "string"
  - schema: "migrations"
    queries: "queries/files"
    engine: postgresql
    gen:
      go:
        package: "filesstore"
        out: "../internal/platform/files/store"
        sql_package: "pgx/v5"
        emit_json_tags: true
        emit_db_tags: true
        emit_pointers_for_null_types: true
        overrides:
          - db_type: "uuid"
            go_type:
              import: "github.com/google/uuid"
              type: "UUID"
          - db_type: "citext"
            go_type: "string"
  - schema: "migrations"
    queries: "queries/notify"
    engine: postgresql
    gen:
      go:
        package: "notifystore"
        out: "../internal/platform/notify/store"
        sql_package: "pgx/v5"
        emit_json_tags: true
        emit_db_tags: true
        emit_pointers_for_null_types: true
        overrides:
          - db_type: "uuid"
            go_type:
              import: "github.com/google/uuid"
              type: "UUID"
          - db_type: "citext"
            go_type: "string"
